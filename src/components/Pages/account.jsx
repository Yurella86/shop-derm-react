import React from 'react';
import Header from '../Header/header';
import { Fragment } from 'react';
import Footer from '../Footer/footer';

const Account = () => {
    return (
        <Fragment>

            <h1 style={{ 'width': 'fit-content', 'margin': '0 auto' }}>Account</h1>

        </Fragment>
    );
};

export default Account;